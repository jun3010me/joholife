# 開発ログ

このファイルには、じょうほうらいふの主要な機能追加・改修の記録を残します。

---

## 2026-09-09 Python プレイグラウンド：変数トレース機能の刷新

### 背景・目的

`/python-playground/` はプロジェクター投影での授業利用を想定して作り変えた。従来の「変数トラッカー」は、Pythonの`sys.settrace`で集めた変数を1つの辞書に上書きしていくだけの実装だったため、**実行完了後の最終状態しか見えず、値がどう変遷したかを追えない**という課題があった。

### 変更内容

1. **変数トレース表示への刷新**（コア機能）
   - 実行中の全ステップ（行の実行ごと）における全変数の値を記録し、値が変化した瞬間だけ強調表示、変化していない値は薄い色で引き継ぎ表示するようにした。
   - 表形式で、共通の「ステップ軸」に沿って全変数を並べる方式（行＝変数/列＝ステップ、または行＝ステップ/列＝変数をチェックボックスで切替可能）。
   - ステップをクリック、または再生コントロール（⏮最初から再生 / ⏪前へ / ▶️⏸再生・一時停止 / ⏩次へ、速度選択）で移動すると、左のコードエディタ側の該当行と、右の変数トレース表の該当列（または行）が同じアクセントカラー（`#2563eb`）でハイライトされ、対応関係が一目でわかるようにした。
   - **既知の制約**: Pyodideはブラウザ上で同期的にPythonコードを実行するため、実行を1行ごとに一時停止してJavaScript側に制御を戻すことはできない。そのため「変数トレースの記録」は実行完了後に一括で行われ、その後の可視化（自動再生・手動ステップ送り）によって「今この行＝この変数値」という対応関係を後追いで確認する方式とした。真の意味でのライブなライン・バイ・ライン実行ではない点に注意。
   - パフォーマンスのため、記録できるステップ数の上限を500に設定（超過時は打ち切り、その旨を警告表示。プログラム自体の実行は最後まで継続される）。

2. **レイアウト変更**
   - 左右50:50のレイアウトは維持しつつ、右側を「実行結果」＋「変数トレース」を縦に並べた構成に変更。
   - ループ実行カウンター・再帰トラッカーは既存のまま、下段の`.debug-panels`に維持。

3. **フォントサイズ調整（独立した2本のスライダー）**
   - コードエディタ用と、変数トレース表示用（出力エリアと共有）で、それぞれ独立したスライダーを用意（連動させない）。
   - CSSカスタムプロパティ `--pp-code-font-size` / `--pp-trace-font-size` を `.python-playground` に持たせて反映。

4. **コードの自動保存・復元（localStorage）**
   - 既存の「💾 保存」「📂 読込」ボタンは、要件定義時の想定と異なりすでに完全に動作していた（`.py`ファイルのダウンロード/アップロード方式）ため、この動作は変更せず維持。
   - 追加で、コード編集内容を`localStorage`に自動保存し、ページ再訪問時に自動復元する機能を実装（単一スロット）。うっかりリロードでコードが消える事故を防ぐための補助機能。

5. **重要な技術的発見: Astroのスコープ付きCSSはJSで動的生成したDOM要素に効かない**
   - Astroの`<style>`はデフォルトでコンポーネントスコープされるが、これは`data-astro-cid-*`属性をビルド時に静的テンプレート要素へ付与する仕組みのため、`innerHTML`などで実行時にJS側から追加した要素にはこの属性が付かず、スコープ付きCSSが適用されない。
   - このページはループ実行カウンター・再帰トラッカー・変数トレース表など、表示内容の大部分を`innerHTML`で動的生成しているため、この問題を回避するために `<style is:global>` に変更した（`Layout.astro`も同じ理由で`is:global`を使用している）。ナビゲーション/フッターとのクラス名衝突がないことは確認済み。

### localStorageキー一覧

| キー | 用途 |
|---|---|
| `pythonPlaygroundCode` | コードの自動保存・復元（単一スロット） |
| `pythonPlaygroundCodeFontSize` | コードエディタのフォントサイズ |
| `pythonPlaygroundTraceFontSize` | 変数トレース表示（＋出力エリア）のフォントサイズ |
| `pythonPlaygroundTraceOrientation` | 変数トレース表の向き（`varsAsRows` / `stepsAsRows`） |
| `pythonPlaygroundPlaySpeed` | 変数トレースの再生速度（ミリ秒） |

命名規則は `markdown-editor.astro` の `editorContent` / `editorLayout` に準拠（JSONに包まない単純な文字列キー）。

### 対象ファイル

- `src/pages/python-playground.astro`（大幅改修）
- `DEVELOPMENT_LOG.md`（新規）
