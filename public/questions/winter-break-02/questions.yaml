id: winter-break-02
title: 冬休み課題②選択構造
description: if文を使ったPythonのコードを読んで、変数の値の変化を追跡し、出力結果を予測する問題です。
icon: 🔀
difficulty: easy
estimatedTime: 10
category: 冬休み課題
tags:
  - Python
  - コードトレース
  - 選択構造
  - if文
questions:
  - id: q1
    type: text-input
    title: 問題1
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      x = 10
      if x > 5:
          x = x + 3
      print(x)
      ```
    answer: "13"
    explanation: if文では、条件式が真の場合にif文の中が実行されます。
    difficulty: easy
    points: 20

  - id: q2
    type: text-input
    title: 問題2
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      a = 7
      if a > 10:
          a = a * 2
      else:
          a = a + 5
      print(a)
      ```
    answer: "12"
    explanation: if-else文では、条件式が偽の場合にelse文の中が実行されます。
    difficulty: easy
    points: 20

  - id: q3
    type: text-input
    title: 問題3
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      x = 15
      y = 10
      if x >= y:
          z = x - y
      else:
          z = y - x
      print(z)
      ```
    answer: "5"
    explanation: if-else文では、条件式の真偽によって実行される文が変わります。
    difficulty: easy
    points: 20

  - id: q4
    type: text-input
    title: 問題4
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      score = 75
      if score >= 80:
          rank = "A"
      elif score >= 60:
          rank = "B"
      else:
          rank = "C"
      print(rank)
      ```
    answer: "B"
    explanation: elif文を使うと、複数の条件を順番に判定できます。
    difficulty: medium
    points: 20

  - id: q5
    type: text-input
    title: 問題5
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      x = 12
      y = 8
      if x > 10:
          if y > 5:
              result = x + y
          else:
              result = x - y
      else:
          result = 0
      print(result)
      ```
    answer: "20"
    explanation: if文は入れ子（ネスト）にして使うことができます。
    difficulty: medium
    points: 20
