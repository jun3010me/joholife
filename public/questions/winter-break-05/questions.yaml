id: winter-break-05
title: 冬休み課題⑤二分探索
description: 二分探索のアルゴリズムを理解し、探索の過程を追跡する問題です。
icon: 🔍
difficulty: medium
estimatedTime: 15
category: 冬休み課題
tags:
  - Python
  - コードトレース
  - 二分探索
  - アルゴリズム
questions:
  - id: q1
    type: text-input
    title: 問題1
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      data = [1, 3, 5, 7, 9]
      target = 5
      left = 0
      right = 4
      count = 0
      found = False

      while left <= right:
          count = count + 1
          mid = (left + right) // 2
          if data[mid] == target:
              found = True
              break
          elif data[mid] < target:
              left = mid + 1
          else:
              right = mid - 1

      print(count)
      ```
    answer: "1"
    explanation: 二分探索では、中央の要素を調べて探索範囲を半分に絞り込んでいきます。
    difficulty: easy
    points: 20

  - id: q2
    type: text-input
    title: 問題2
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      data = [2, 5, 8, 12, 16, 23, 38]
      target = 23
      left = 0
      right = 6
      count = 0

      while left <= right:
          count = count + 1
          mid = (left + right) // 2
          if data[mid] == target:
              break
          elif data[mid] < target:
              left = mid + 1
          else:
              right = mid - 1

      print(count)
      ```
    answer: "2"
    explanation: 探索範囲を半分ずつ絞り込んでいくため、効率的に目的の値を見つけることができます。
    difficulty: easy
    points: 20

  - id: q3
    type: text-input
    title: 問題3
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      data = [1, 4, 7, 10, 13, 16, 19]
      target = 15
      left = 0
      right = 6
      result = -1

      while left <= right:
          mid = (left + right) // 2
          if data[mid] == target:
              result = mid
              break
          elif data[mid] < target:
              left = mid + 1
          else:
              right = mid - 1

      print(result)
      ```
    answer: "-1"
    explanation: 二分探索では、目的の値が見つからない場合、特定の値（この例では-1）を返します。
    difficulty: medium
    points: 20

  - id: q4
    type: text-input
    title: 問題4
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      data = [3, 6, 9, 12, 15, 18, 21, 24, 27]
      target = 27
      left = 0
      right = 8
      count = 0

      while left <= right:
          count = count + 1
          mid = (left + right) // 2
          if data[mid] == target:
              break
          elif data[mid] < target:
              left = mid + 1
          else:
              right = mid - 1

      print(count)
      ```
    answer: "4"
    explanation: データ数が増えても、二分探索では比較的少ない回数で目的の値を見つけることができます。
    difficulty: medium
    points: 20

  - id: q5
    type: text-input
    title: 問題5
    description: |
      以下のPythonコードを実行すると、どんな値が出力されますか？
      ```python
      data = [5, 10, 15, 20, 25, 30, 35]
      target = 35
      left = 0
      right = 6
      count = 0
      found = False

      while left <= right:
          count = count + 1
          mid = (left + right) // 2
          if data[mid] == target:
              found = True
              break
          elif data[mid] < target:
              left = mid + 1
          else:
              right = mid - 1

      if found:
          print(count)
      else:
          print(-1)
      ```
    answer: "3"
    explanation: 最後の要素を探す場合でも、二分探索は効率的に動作します。
    difficulty: medium
    points: 20
